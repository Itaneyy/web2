export declare const pluginPackageName = "@poliklot/prettier-plugin-handlebars";
export declare const handlebarsParserName = "handlebars";
export type InitChangeKind = 'create' | 'update';
export type InitWarningKind = 'unsupported-config' | 'prettierignore' | 'dependency' | 'invalid-config';
export interface InitChange {
    kind: InitChangeKind;
    filePath: string;
    relativePath: string;
    message: string;
    content: string;
}
export interface InitWarning {
    kind: InitWarningKind;
    filePath?: string;
    relativePath?: string;
    message: string;
    checkFailure: boolean;
}
export interface InitPlan {
    cwd: string;
    configPath?: string;
    configRelativePath?: string;
    changes: InitChange[];
    warnings: InitWarning[];
    alreadyConfigured: boolean;
    installCommand?: string;
}
export declare function createInitPlan(cwd?: string): InitPlan;
export declare function applyInitPlan(plan: InitPlan): void;
export declare function hasCheckFailures(plan: InitPlan): boolean;
export declare function renderInitPlan(plan: InitPlan, mode: 'dry-run' | 'write' | 'check'): string;
