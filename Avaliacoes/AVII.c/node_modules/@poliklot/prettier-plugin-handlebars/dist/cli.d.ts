#!/usr/bin/env node
interface CliOptions {
    command?: string;
    cwd: string;
    write: boolean;
    check: boolean;
    help: boolean;
}
declare function main(argv: string[], executableName?: string): number;
declare function parseArgs(argv: string[], defaultCommand?: string): CliOptions;
declare function getDefaultCommand(executableName: string): string | undefined;
declare function renderHelp(executableName?: string): string;
export { main, parseArgs, renderHelp, getDefaultCommand };
